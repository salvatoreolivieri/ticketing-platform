{
  "name": "@ticketing/catalog",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "./src/server.ts",
  "scripts": {
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:seed": "tsx src/infrastructure/db/seed.ts"
  },
  "dependencies": {
    "@ticketing/shared": "*",
    "cors": "^2.8.6",
    "express": "^4.19.2"
  }
}
